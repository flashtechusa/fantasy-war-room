"""Yahoo Fantasy Sports support.

Yahoo is a second *platform*, not a second projection source: it answers the
same questions ESPN does (league rules, teams, rosters, draft) through a
completely different API, so it lives behind the same `DataProvider` protocol
and everything downstream is unchanged.
"""
